import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vicdata.paws',
  appName: 'paws',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
